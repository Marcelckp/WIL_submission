android.useAndroidX=true
android.enableJetifier=true
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8

# Work around jlink transform issue on some JDK builds
android.experimental.disableJdkImageTransform=true

# API Configuration - can be overridden via environment variable or .env file
# Local development: Use 10.0.2.2 for Android emulator (localhost)
# For physical device, use your computer's IP address (e.g., http://192.168.1.XXX:3000/api/)
# Production: Change to https://wilsubmission-production.up.railway.app/api/
API_BASE_URL=http://10.0.2.2:3000/api/
